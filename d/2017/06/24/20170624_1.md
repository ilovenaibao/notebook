# 搜索工具rg(ripgrep)
`+` `tool` `link` `qq群`

http://blog.burntsushi.net/ripgrep/

git://github.com/BurntSushi/ripgrep

SpaveVim 群推荐，wsdjeg 说在写 vim 插件

两种正则引擎：
支持回退，有些情况很慢
不支持回退

两类搜索工具：
grep 系：指定目标文件
ack 系：默认遍历文件夹，忽略版本控制目录

两类用途：
大工程目录树中搜索
大日志单文件中搜索

rg 用 rust 语言写的，没听过

并行搜索
搜索大目录中许多小文件，用文件内存映射并不快

这文章比较以下搜索工具：
ripgrep
gun grep
git grep
The Silver Searcher(ag)
UniversalCode Grep(ucg)
The Platinum Searcher(pt)
shift

ack 说太慢了，不参与比较，它用 perl 写的
ag 是 C 版本的 ack
ucg 是 C++ 版本的 ack

rust 系统编程语言
https://www.rust-lang.org
